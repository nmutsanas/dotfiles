[module/filesystem]
type = internal/fs
interval = 25

mount-0 = /
mount-1 = /mnt/data
mount-2 = /mnt/ds-share

label-mounted = %mountpoint% %free% (%percentage_used%%)
label-unmounted = %mountpoint% (?)
label-unmounted-foreground = ${colors.faded_yellow}
